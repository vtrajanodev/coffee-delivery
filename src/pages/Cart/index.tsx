import { Address } from "../../components/Address"
import { CartContainer } from "./styles"

export const Cart = () => {
  return (
    <CartContainer>
      <Address />
      test
    </CartContainer>
  )
}
